# L4 分析决策层的**开发与门禁**依赖（锁定版本：便于复现与升级审查）
# 依据：docs/design/language.md TB-15（CI 必须含 Python 的 ruff 检查）
# 安装：make pyenv（建在 analysis/.venv，不污染系统 Python）
#
# grpcio-tools：生成 gRPC 桩（make pygen）。桩入库，所以它只在重新生成时需要。
ruff==0.16.8
pytest==9.1.1
grpcio-tools==1.84.0
