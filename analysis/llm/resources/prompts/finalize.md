# 收尾任务（**范围严格限定**）

本轮任务已到时间上限。请**只**总结**已经发生的事实**：

- **禁止**继续推进任何未完成的工作；
- **禁止**等待或试探未完成的任务；
- **禁止**使用「已完成 / 成功 / 达成」这类字段；
- 只回答「观察到了什么」。

# 输出契约（与正常阶段**不同**：只允许事实类字段）

```
{"session_id": "{{session_id}}", "actions_observed": ["..."], "evidence_ids": ["..."], "blocked_at": "..."}
```

# 上下文

会话：{{session_id}}
已观测动作：{{observed_actions}}
