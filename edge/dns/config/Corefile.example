# 形态 ② · DNS 智能解析引流 —— CoreDNS 配置模板
#
# 依据：INT-2（按来源解析到引擎或真实服务）· AR-3（L0 禁止自研）· INT-25（白名单）· NI-1
# 设计说明见 docs/modules/adapter-dns.md
#
# ⚠️⚠️ 顺序即语义：带 view 的 server block **必须**写在兜底 block 之前。
#      若兜底在前，它会先匹配一切，带 view 的块永远不会被命中 —— 且**不报任何错**。
#      现象是「配置看起来生效了，分流完全不发生」。
#
# ⚠️ 这个形态的 NI-1 风险高于其他三种：一旦解析被接管，引擎就是业务可达性的前置条件。
#    启用前必须确认下面「回退开关」一节的三条。

# ── 块 1：可疑来源 → 引擎 ────────────────────────────────────────────────────
example.com:53 {
    # 只对白名单**之外**的可疑网段生效。内部 IP 与探针不该被引到这里（INT-25）。
    view suspicious {
        expr incidr(client_ip(), '203.0.113.0/24')
    }

    # 引擎 IP。实际部署时指向形态③的 adapter-proxy，由它决定放行还是改道。
    template IN A example.com {
        answer "{{ .Name }} 30 IN A 10.0.0.99"
    }

    # TTL 取短：规则变更必须能快速生效，回退也必须快（见下方回退开关）。
    cache 30
}

# ── 块 2：其余来源 → 真实业务 ───────────────────────────────────────────────
# 兜底块。**必须在最后。**
example.com:53 {
    forward . 8.8.8.8 1.1.1.1
    cache 30
}

# ── 回退开关（不满足这三条禁止启用本形态）──────────────────────────────────
#
# 1. 只接管白名单外的**指定域名**，默认不接管任何域名；
# 2. 引擎侧必须有可用的放行路径（形态③的 adapter-proxy，或真实业务本身）；
# 3. 能一键回退：把引擎的 A 记录改回真实业务 IP，且 TTL 短到能快速生效。
#
# 回退操作：停用块 1（注释掉或删掉），块 2 立即接管全部来源。
# 生效时间受 TTL 与客户端/递归解析器缓存影响 —— 这就是 TTL 必须短的原因。
#
# ── 验收 ─────────────────────────────────────────────────────────────────
#
#   dig @<coredns-ip> example.com +short                       # 本机来源 → 真实业务
#   dig @<coredns-ip> -b 203.0.113.7 example.com +short        # 可疑来源 → 引擎 IP
#
# 从两个来源网段查询同一域名，必须得到不同结果。这是本形态唯一的验收标准。
# 该检查应作为 shen doctor（INT-17）检查项 ⑤ 的一部分。
