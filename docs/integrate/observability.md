# 看告警 · 看流量 · 看流动

## 数据从哪来

```text
客户端 → L0（客户 LB/nginx，终结 TLS）→ 适配器（白名单 → 判定缓存 → gRPC 判定）
        → 核心（judge 打分 → director 三值）→ 按决策执行（放行 / 改道+注入 / 拦截）
        → 异步遥测（事件写回核心）→ 观测控制台读出来
```

- **判定记录**（含分值 / 命中信号 / 去向）由核心在**服务面**记一笔（`core/internal/control` 的观测面记录器）；
- **适配器侧的行为**（是否影子、是否引流回落）由适配器异步上报成遥测事件；
- 控制台**只读**，不写策略、不干预请求（`AR-10`：控制面禁止参与请求级判定）。

## 控制台

```sh
make console            # 只起控制台（核心需已在跑；核心地址取 SHEN_CORE_ADDR）
scripts/demo/run.sh     # 或连核心一起起
```

页面四块（自上而下）：

| 区块 | 回答什么 |
| --- | --- |
| **概览** | 事件总数 · 按决策分布（放行/改道/拦截）· 告警条数 · 时间范围 |
| **告警** | 决策为 `block` 或 `severity` 非 `none` 的记录（**影子模式下通常为空 —— 这是对的**） |
| **流量访问与流动** | 最近判定：时间 · 决策 · 来源 IP · 方法 · 路径 · UA · **分值** · **命中信号** · 后端名；可按决策筛选 |
| **分析结论（L4）** | 近线分析（意图 / 策略）的结论：类型 · 是否接受 · 结论摘要或**拒绝原因** · 分析条数 · 证据引用数 |
| **最近事件（原始）** | 未加工的遥测事件流（排障时看原始形状） |

### 让「分析结论」里有东西

```sh
SHEN_CORE_ADDR=127.0.0.1:9443 make analysis   # 跑一轮 L4（近线，不在业务路径上）
```

L4 需要**新态势**才会出结论：同一 `(来源, 会话, 方法, 路径)` 在 60 秒窗口内只分析一次（`AR-14`）。
所以「造一批新流量 → 再跑一轮」才会看到新结论。

JSON 接口（页面的数据源，也方便脚本化）：

| 接口 | 用途 |
| --- | --- |
| `GET /api/summary` | 概览 |
| `GET /api/flow?limit=300` | 判定流动（含分值 / 信号 / 后端） |
| `GET /api/events?limit=100&type=decision` | 原始事件（`type` 可空；`type=analysis` 只看 L4 结论） |
| `GET /api/analysis` | **L4 结论**（意图 / 策略；含拒绝原因与证据引用数） |
| `GET /healthz` | 控制台进程存活（**不**代表核心可达 —— `ST-17` 的探针语义区分） |

## 三条判读要点

1. **分数与信号只在这里可见**：判定响应**禁止**回显它们（`ST-7`），所以「为什么判成这样」只能看控制台或 `make replay`。
2. **影子模式下没有 `block`**：看到「告警 0 条」不代表没抓到东西，去看**分值**与**命中信号**。
3. **时间**：页面按浏览器本地时区显示；JSON 里 `created_at` 是 UTC、事件载荷的 `at` 带本地偏移 —— 同一时刻，别被两个时区吓到。
4. **L4 结论不是判定**：`分析结论（L4）` 只回答「想干什么」（近线），不参与请求级判定（`AR-10`/`AR-32`）；`strategy` 被拒是**正常**的 —— 没有登记诱饵时它按 `AR-15` 拒绝而不是编一个。

## 5. 看逐请求链路（DAG）

页面第二块。**每条请求一张独立的小图**（不聚合），随页面每 5 秒刷新 —— 新流量出现在最上面。

**关键是把两件事分开读**（图上的节点会同时写出两者）：

| 概念 | 来源 | 含义 |
| --- | --- | --- |
| **意图**（图上节点「核心判定」之后的边） | 核心的 `decision` 事件 | 引擎判成什么（放行 / 改道 / 拦截）+ 分值 + 命中信号 |
| **实际**（边上的 `executed`） | 适配器的 `request_judged` 事件 | 真正走到哪：业务源站 · 幻境后端 · 拦截 · 白名单直通 · 缓存复用 · 判定失败放行 |

- **影子模式下两者会不一致**（判为改道、实际仍走源站）—— 这是设计（`INT-11`），页面顶部会提示；
- **幻境回落**（`NI-5`）在图上是一条**虚线**边：`幻境不可用 → 回落业务`；
- **告警两段**：🔴=真实告警（`block` 或 `severity≠none`）· 🟠=高风险（分值 ≥ 阈值，**仅显示**，不改变处置）；
- **每跳都带该请求自己的值**：适配器上写 `方法 路径`、核心判定上写「分值 + 命中信号」、业务源站上写「状态码 · 字节 · 耗时」；
- **点卡片** → 四段详情（请求 / 判定 / 执行与返回 / 告警），`未采集` 表示该字段当前没有数据；
- **筛选**：全部 / 只看告警 / 只看高风险 / 只看幻境与回落 / 只看源站。
- （程序化用途仍可用 `GET /api/topology` 取聚合视图；页面不再用它。）
