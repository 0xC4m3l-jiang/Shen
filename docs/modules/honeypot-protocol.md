# 模块：`honeypot-protocol`

| 项 | 内容 |
| --- | --- |
| 模块名 | `honeypot-protocol` |
| 所属层 | `L2`（依据 MD-1） |
| 实现语言 | `Go` / `Rust`（依据 [`../design/language.md`](../design/language.md) §1） |
| 负责人 | — |
| 状态 | ✅ **框架已实现**（阶段 3，`modules/honeypot/protocol/`，7 例单测）：注册表 · 连接上限 · 对称回收 · 会话录制口 · 一个最小真实适配器（banner）。**真实协议栈是核心逻辑接缝**（见 §8） |
| 最后更新 | 2026-09-19 |

---

## 1. 职责

**做什么**：

- **协议仿真**：SSH / MySQL / Redis / FTP / ElasticSearch / nginx-admin 等真实协议栈的假服务；
- **交互捕获**：逐命令 / 逐查询转录，凭证捕获，文件投递记录。

**明确不做什么**：

- **不做判定**（`AR-2`）· **不做决策**（`MD-12`）；
- **不主动连接任何目标** —— 它只承接 `honeypot` 路由来的流量（`SB-6`）；
- **不与判别同进程** —— 独立进程 / 容器（`MD-4` 依赖方向）。
- ⚠️ **本模块是可选自研**：默认由 `honeypot` 接第三方蜜罐（[ADR-0011](../background/decisions/0011-honeypot-entry-external-backends.md)）。

## 2. 输入 / 输出契约

| 方向 | 契约 | 定义位置 |
| --- | --- | --- |
| 输入 | 承接 `honeypot` 路由来的流量（协议端口） | [`../modules/honeypot.md`](honeypot.md) |
| 输出 | 事件（会话转录 / 凭证捕获 / 文件投递） | `common/api/telemetry/v1` |

## 3. 依赖

| 允许依赖 | 原因 |
| --- | --- |
| 协议库（paramiko 类等价物） | 真协议栈 |

| 禁止依赖 | 原因 |
| --- | --- |
| 判定 / 决策 | `AR-2` / `MD-12` |
| 核心内部包 | 依赖方向单向（`MD-4`） |

## 4. 关键规则

| 规则 | 与本模块的关系 |
| --- | --- |
| `MD-14` | 子进程**必须**按进程组回收 + 双层超时 |
| `MD-15` | 资源创建与释放**必须**对称 |
| `MD-16` | 并发连接数 / 线程数**必须**设上限 |
| `NI-7` | **必须**受 cgroup 上限约束 |
| `NI-13` | 会话转录按类别配置保留天数 |
| `AR-25` | 会话 ID 作为一等字段，建立 / 心跳 / 收尾一致 |
| `SB-6` | **禁止**主动连接目标 |

## 5. 状态与生命周期

| 状态 | 存在哪里 | 生命周期 | 多副本一致性 |
| --- | --- | --- | --- |
| 会话转录 | `store`（事件） | 按保留策略 | 强一致 |
| 凭证捕获 | `store`（凭证观察库） | 按保留策略 | 强一致 |
| 运行态 | 进程内（连接 / 子进程） | 会话期 | 各自独立 |

## 6. 失败模式与降级

| 失败情形 | 行为 | 是否满足 `NI-1` | 依据 |
| --- | --- | --- | --- |
| 子进程超时 | 按进程组 `SIGTERM` → 宽限 → `SIGKILL` | ✅ | `MD-14` |
| 连接数超限 | 拒绝并记录 | ✅ | `MD-16` |
| 蜜罐不可达 | `route_mirage` 回落业务（`honeypot` 侧） | ✅ | `NI-5` |

## 7. 测试

| 类型 | 覆盖什么 | 位置 |
| --- | --- | --- |
| 单元 · 注册表 | nil / 空名 / 重名必须报错（名字唯一是「谁生效」的前提） | `modules/honeypot/protocol/protocol_test.go` |
| 单元 · 适配器 | banner 问候 + 双向录制 + 会话 ID 非空（`AR-25`） | 同上 |
| 单元 · 并发上限 | 超限连接被拒并计数（`MD-16`） | 同上 |
| 单元 · 生命周期 | `Stop` 在宽限期内返回 · 监听关闭 · 重复启动与未知协议报错（`MD-15`） | 同上 |
| 单元 · 接缝 | 「尚未实现」必须**显式报错**，不静默返回空结果 | 同上 |
| 专项（待补） | 假可执行文件启动真子进程，验证**进程组回收**（`MD-14`）—— 仅适用于子进程形态的蜜罐 | 待补（接第三方蜜罐时） |
| 压力（待补） | 长时压测下的连接上限与回收稳定性 | 待补 |

## 8. 未决项

| # | 未决 | 阻塞什么 | 去向 |
| --- | --- | --- | --- |
| 1 | **是否自研**（vs 接第三方） | 整个模块的存在必要性 | [ADR-0011](../background/decisions/0011-honeypot-entry-external-backends.md) |
| 2 | 协议覆盖范围（哪些协议必做） | 实现排期 | 阶段 3 设计 |
| 3 | ⚠️ **真实协议栈未实现**（SSH 密钥交换 / MySQL 握手 / Redis RESP / 凭证捕获与命令解释）—— 这是本模块的**核心逻辑接缝**：框架与契约已就绪，新协议只需满足 `Protocol` 再 `Register` | 蜜罐的「像不像真的」全部取决于它 | 阶段 3 设计（或接第三方，[ADR-0011](../background/decisions/0011-honeypot-entry-external-backends.md)） |
| 4 | `MD-14` 的**进程组回收**只适用于子进程形态：本框架跑进程内适配器，能做的上限是「关连接 + 等宽限」 | 接第三方可执行蜜罐时必须有进程组管理（那份责任在 `honeypot` 后端池或本模块的进程包装层） | 接第三方时定 |

## 9. 变更记录

| 日期 | 变更 | 依据 |
| --- | --- | --- |
| 2026-09-18 | 创建（设计）：协议仿真 + 交互捕获；定位为**可选自研** | [ADR-0011](../background/decisions/0011-honeypot-entry-external-backends.md) |
| 2026-09-19 | **框架落地**：`Protocol` / `Session` / `Registry` 契约 + `Runner`（并发上限 `MD-16` · 对称回收 `MD-15`）+ `Banner` 最小真实适配器；7 例单测；协议栈留作核心逻辑接缝 | [`../plans/2026-09-19-deception-l2-l3-frameworks.md`](../plans/ARCHIVE.md) |
