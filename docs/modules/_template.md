# 模块：`<module-name>`

> 模板。复制为 `docs/modules/<module-name>.md` 后按章节填写。
> 规则 MD-1…MD-17 见 [`../design/modules.md`](../design/modules.md) §2。
>
> **一节都不许删。** 不适用时写「不适用」并说明原因 —— 留空与不适用的区别必须写清楚。

| 项 | 内容 |
| --- | --- |
| 模块名 | `<kebab-case>` |
| 所属层 | `L0` / `L1` / `核心` / `L2` / `L3` / `L4` / `安全底座` / `控制台`（依据 MD-1：一个模块只属于一层） |
| 实现语言 | `Go` / `Rust` / `Python` / `TypeScript`（依据 [`../design/language.md`](../design/language.md) §1） |
| 负责人 | |
| 状态 | 设计 / 实现中 / 已上线 |
| 最后更新 | YYYY-MM-DD |

---

## 1. 职责

**做什么**（一句话 + 要点列表）：

-

**明确不做什么**（边界，**必须**写；写不出说明边界没想清）：

-

> 规则 MD-1：本模块**不得**跨层拆分。若发现需要跨，说明模块划分错了，先改划分。
>
> ⚠️ 若本模块属于 `L1` / 适配器：**禁止**实现判定逻辑、**禁止**做 LLM 推理、**禁止**维护会话状态、
> **禁止**写入核心状态（MD-9 / AR-6 / AR-7）。

## 2. 输入 / 输出契约

**跨模块与对外的契约不写在这里**（规则 MD-3），只引用：

| 方向 | 契约 | 定义位置 |
| --- | --- | --- |
| 输入 | | `../spec/…` 或 `api/` 的 `.proto` |
| 输出 | | `../spec/…` 或 `api/` 的 `.proto` |

本模块内部的数据结构：

-

> 规则 MD-5：跨模块共享的类型**必须**收敛到一处 —— **跨进程**契约放 `spec/` + `api/`，**进程内**共享类型放 `contract/`；**禁止**各自定义同名类型。

## 3. 依赖

| 允许依赖 | 原因 |
| --- | --- |
| | |

| 禁止依赖 | 原因 |
| --- | --- |
| | |

> 规则 MD-4：依赖方向**必须**单向（适配器 → 核心 → 数据面）。
> 核心模块**禁止**外呼、**禁止**写业务存储、**禁止**依赖系统时钟做判定（MD-6）。

## 4. 关键规则

必须遵守的上位规则（引用 [`../design/`](../design/README.md) 的 ID）：

| 规则 | 与本模块的关系 |
| --- | --- |
| `AR-…` | |
| `MD-…` | |
| `NI-…` | |
| `OH-…` | |
| `ST-…` | |
| `TB-…` | |
| `TM-…` | |

## 5. 状态与生命周期

| 状态 | 存在哪里 | 生命周期 | 多副本一致性 |
| --- | --- | --- | --- |
| | | | |

> 规则 AR-9：核心**必须**无状态多副本，状态**必须**全部外置。

## 6. 失败模式与降级

| 失败情形 | 行为 | 是否满足 `NI-1` | 依据 |
| --- | --- | --- | --- |
| 输入非法 | | | `NI-5`（回落放行到真实业务） |
| 超时 | | | `NI-4` |
| 外部依赖不可用 | | | `NI-3` / `NI-10` |
| 资源耗尽 | | | `NI-7` |
| 输出不合格（LLM） | | | `AR-15` / `AR-21` |

## 7. 测试

| 类型 | 覆盖什么 | 位置 |
| --- | --- | --- |
| 单元 | | |
| 集成 | | |
| 故障注入 | | `NI-12` 的 `V-1…V-5` 中与本模块相关的项 |
| 属性测试 | | |
| 分支穷尽性 | | `MD-8`（判定器必填） |

## 8. 未决项

| # | 未决 | 阻塞什么 | 去向 |
| --- | --- | --- | --- |
| | | | [`../background/notes/`](../background/notes/) 或 [`../background/decisions/`](../background/decisions/README.md) |

## 9. 变更记录

| 日期 | 变更 | 依据 |
| --- | --- | --- |
| | | |
