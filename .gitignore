# 忽略清单（依据 ST-20：含密钥的配置文件必须加入忽略清单；工具产物不进版本库）
#
# ⚠️ 本仓库当前**还不是 git 仓库**（无版本控制）。这份清单现在就写好，是为了
#    第一次 `git init` 时不把密钥与工具产物带进去 —— 事后清理历史比事前忽略贵得多。

# ── 密钥与真实配置（ST-20 / ST-21）─────────────────────────────────────────
# 配置模板（*.example.*）入库；由 SHEN_CONFIG 指向的真实配置**禁止**入库。
deploy/config/config.yaml
deploy/config/*.local.yaml
*.secret.yaml
*.key
*.pem
.env
.env.local

# ── 门禁工具与产物（与 .bin/ 同理：版本固定但不入库）───────────────────────
.bin/

# ── 构建与运行产物 ────────────────────────────────────────────────────────
/core/core
/core/cmd/core/core
/edge/proxy/cmd/proxy/proxy
*.test
coverage.out

# ── 编辑器与系统 ──────────────────────────────────────────────────────────
.DS_Store
*.swp
