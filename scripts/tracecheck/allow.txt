# tracecheck 的已知缺口登记（显式豁免）
#
# 每行：<检查ID> <目标>  # 理由
#
# 规矩：
#   1. 只登记**已知且有理由**的缺口，不登记「懒得修」；
#   2. 豁免**会腐烂** —— 一旦该问题不再出现，tracecheck 会报「豁免已过期」，
#      那时必须删掉这一行（不留僵尸条目）；
#   3. 缺口清单在 `make gate` 的输出里始终打印，不允许悄悄变长。
#
# 当前 1 条豁免：
#
# D-3 ST-25 —— structure.md §1.4 那句是「若要把这条约定升格，加一条 ST-25」的**假设句**，
#            不是对已有规则的引用。升格成真规则后（或改写该句后）必须删掉本行。
D-3 ST-25  # docs/design/structure.md §1.4：尚未升格为规则的约定，写作假设句而非引用
